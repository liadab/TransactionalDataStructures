To make the scripts executable, execute:
    chmod 755 compile run

To compile, execute:
    ./compile

The ``run`` script invokes the JVM with some recommended parameters.

to run, execute (from within THIS directory):
    ./run [n_threads] [n_tasks] [n_tasks_per_tx] [x_of_100_inserts] [x_of_100_removes]