//#pragma once
//
//#include <unordered_map>
//#include <unordered_set>
//
//#include "nodes/LNode.h"
//#include "WriteElement.h"
//
//class LocalStorage {
// long readVersion = 0L;
// long writeVersion = 0L; // for debug
// bool TX = false;
// bool readOnly = true;
// //TODO add when queue is implmentd
// //std::map<Queue, LocalQueue> queueMap = new HashMap<Queue, LocalQueue>();
//    std::unordered_map<LNode, WriteElement> writeSet = new HashMap<LNode, WriteElement>();
//    std::unordered_set<LNode> readSet = new HashSet<LNode>();
//    std::unordered_map<LinkedList, ArrayList<LNode>> indexAdd = new HashMap<LinkedList, ArrayList<LNode>>();
//    std::unordered_map<LinkedList, ArrayList<LNode>> indexRemove = new HashMap<LinkedList, ArrayList<LNode>>();
//};
//
